/** Mobile breakpoint in pixels — matches CSS media queries */
export const MOBILE_BREAKPOINT = 768;

/** API cache TTLs in milliseconds */
export const CACHE_TTL_MS = 5 * 60 * 1000; // 5 minutes
export const MACHINES_CACHE_TTL_MS = 24 * 60 * 60 * 1000; // 24 hours
